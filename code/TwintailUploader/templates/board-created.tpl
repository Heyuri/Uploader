<div class="boardCreatedPage centerItem">
	<h3>{{lang.boards.createdHeading}}</h3>
	<div class="aiHelper">
		<div class="aiImage">
			<img src="{{aiYayUrl}}" width="200" height="150" alt="">
		</div>
		<i class="aiComment">{{lang.boards.createdComment}}</i>
	</div>

	<p class="boardCreatedAddress">[<a href="{{boardUrl}}">{{boardUri}}</a>]</p>
	<p class="redText">{{lang.boards.createdPasswordWarning}}</p>

	<div class="utilityOptions">
		<a href="{{boardUrl}}">{{lang.boards.visitBoard}}</a> |
		<a href="{{boardsUrl}}">{{lang.boards.title}}</a>
	</div>
</div>
